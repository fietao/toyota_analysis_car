Have a export button and will able to pick the data when cliking it like what you want to export I can thick it and then export it to a csv(excel) file.
also don't forget to add a filter data that alredy source by the program by the cleaned data 